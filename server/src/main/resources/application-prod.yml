spring:
  config:
    activate:
      on-profile: prod

  jpa:
    properties:
      show-sql: false

server:
  port: ${PORT}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
